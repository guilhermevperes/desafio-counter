
const initialValue = {
  primaryColor: '#162F57',
  secondaryColor: '#FFFFFF',
  tertiaryColor: '#05D3D6',
  quaternaryColor: '#0082C9',
  quinaryColor: '#00984b',
  senaryColor: '#8a0101',
  septenaryColor: '#b9b9b9'
}

function themeReducer (state, action) {
  switch (action.type) {
    default:
      throw new Error()
  }
}

export { themeReducer, initialValue }
