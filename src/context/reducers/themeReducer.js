
const initialValue = {
  primaryColor: '#14BCD2',
  secondaryColor: '#000000'
}

function themeReducer (state, action) {
  switch (action.type) {
    default:
      throw new Error()
  }
}

export { themeReducer, initialValue }
