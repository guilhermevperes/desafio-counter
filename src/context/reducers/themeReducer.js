
const initialValue = {
  primaryColor: '#162F57',
  secondaryColor: '#DCDCDC'
}

function themeReducer (state, action) {
  switch (action.type) {
    default:
      throw new Error()
  }
}

export { themeReducer, initialValue }
